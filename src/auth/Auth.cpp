#include "./../../include/auth/Auth.h"

bool signUp(const std::string& username, const std::string& password, UserRole userRole) {
 // Read user_data.json file and check if user exists


 // If new user, save data into json file

 return true;
}

// Sign in a user, creating a session
bool signIn(const std::string& username, const std::string& password) {
  // Check if username exists
  // If no, return false

  // If true, check passwords matching

  // If false, return false

  return true;
}
